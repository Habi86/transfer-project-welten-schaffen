# This is a custom document node type which has been auto-generated for your site package
# by Neos. It works exactly the same as the generic "Neos.NodeTypes:Page". You can customize this
# to your needs or remove it if you do not need a custom page type.
'Vendor.WeltenSchaffenAt:Page':
  superTypes:
    'Neos.Neos:Document': true
  ui:
    icon: 'icon-home'
    label: 'WeltenSchaffenAt Home'
  properties:
    box-1-initial:
      type: string
      ui:
        label: 'box-1-initial'
        inlineEditable: TRUE
        reloadIfChanged: TRUE
        inline:
          editorOptions:
            placeholder: 'Enter initial...'
    box-1-txt:
      type: string
      ui:
        label: 'box-1-txt'
        inlineEditable: TRUE
        reloadIfChanged: TRUE
        inline:
          editorOptions:
            placeholder: 'Enter text...'
            formatting:
              left: TRUE
              center: TRUE
              right: TRUE
              strong: TRUE
              em: TRUE
        # inspector:
        #   group: 'document'
        #   editor: 'Neos.Neos/Inspector/Editors/TextAreaEditor'
        #   editorOptions:
        #     rows: 7
        #     formatting:
        #       bold: TRUE
    box-1-passingWord:
      type: string
      ui:
        label: 'box-1-passingWord'
        inlineEditable: TRUE
        reloadIfChanged: TRUE
        inline:
          editorOptions:
            placeholder: 'Enter passing word...'
    box-3-txt2:
      type: string
      ui:
        label: 'box-3-txt2'
        inlineEditable: TRUE
        reloadIfChanged: TRUE
        inline:
          editorOptions:
            placeholder: 'Enter text... must contain 8 lines'
            formatting:
              left: TRUE
              center: TRUE
              right: TRUE
              strong: TRUE
              em: TRUE
    box-4-centered-txt:
      type: string
      ui:
        label: 'box-4-centered-txt'
        inlineEditable: TRUE
        reloadIfChanged: TRUE
        inline:
          editorOptions:
            placeholder: 'Enter text...'
            formatting:
              left: TRUE
              center: TRUE
              right: TRUE
              strong: TRUE
              em: TRUE
    box-5-title:
      type: string
      ui:
        label: 'box-5-title'
        inlineEditable: TRUE
        reloadIfChanged: TRUE
        inline:
          editorOptions:
            placeholder: 'Enter title...'
            formatting:
              left: TRUE
              center: TRUE
              right: TRUE
              strong: TRUE
              em: TRUE
    box-5-txt:
      type: string
      ui:
        label: 'box-5-txt'
        inlineEditable: TRUE
        reloadIfChanged: TRUE
        inline:
          editorOptions:
            placeholder: 'Enter text...'
            formatting:
              left: TRUE
              center: TRUE
              right: TRUE
              strong: TRUE
              em: TRUE
    box-6-txt:
      type: string
      ui:
        label: 'box-6-txt'
        inlineEditable: TRUE
        reloadIfChanged: TRUE
        inline:
          editorOptions:
            placeholder: 'Enter text...'
            formatting:
              left: TRUE
              center: TRUE
              right: TRUE
              strong: TRUE
              em: TRUE
    box-9-initial:
      type: string
      ui:
        label: 'box-9-initial'
        inlineEditable: TRUE
        reloadIfChanged: TRUE
        inline:
          editorOptions:
            placeholder: 'Enter initial...'
    box-9-txt:
      type: string
      ui:
        label: 'box-9-txt'
        inlineEditable: TRUE
        reloadIfChanged: TRUE
        inline:
          editorOptions:
            placeholder: 'Enter text...'
            formatting:
              left: TRUE
              center: TRUE
              right: TRUE
              strong: TRUE
              em: TRUE
  childNodes:
    main:
      type: 'Neos.Neos:ContentCollection'

'Vendor.WeltenSchaffenAt:Mini':
  superTypes:
    'Neos.Neos:Document': true
  ui:
    icon: 'icon-compress'
    label: 'WeltenSchaffenAt Mini'
  properties:
    header:
      type: string
      ui:
        label: 'header'
        inlineEditable: TRUE
        reloadIfChanged: TRUE
        inline:
          editorOptions:
            placeholder: 'Enter title...'
            formatting:
              left: TRUE
              center: TRUE
              right: TRUE
              strong: TRUE
              em: TRUE
    contentheader:
      type: string
      ui:
        label: 'contentheader'
        inlineEditable: TRUE
        reloadIfChanged: TRUE
        inline:
          editorOptions:
            placeholder: 'Enter title...'
            formatting:
              left: TRUE
              center: TRUE
              right: TRUE
              strong: TRUE
              em: TRUE
    content:
      type: string
      ui:
        label: 'content'
        inlineEditable: TRUE
        reloadIfChanged: TRUE
        inline:
          editorOptions:
            placeholder: 'Enter contacts...'
            formatting:
              left: TRUE
              center: TRUE
              right: TRUE
              strong: TRUE
              em: TRUE
              a: TRUE
    extrainfo:
      type: string
      ui:
        label: 'extrainfo'
        inlineEditable: TRUE
        reloadIfChanged: TRUE
        inline:
          editorOptions:
            placeholder: 'Optional...'
            formatting:
              left: TRUE
              center: TRUE
              right: TRUE
              strong: TRUE
              em: TRUE

# TODO: Make target_blank working when editing URLS then remove this:
'Vendor.WeltenSchaffenAt:MiniTmp':
  superTypes:
    'Neos.Neos:Document': true
  ui:
    icon: 'icon-compress'
    label: 'WeltenSchaffenAt MiniTmp'
  properties:
    header:
      type: string
      ui:
        label: 'header'
        inlineEditable: TRUE
        reloadIfChanged: TRUE
        inline:
          editorOptions:
            placeholder: 'Enter title...'
            formatting:
              left: TRUE
              center: TRUE
              right: TRUE
              strong: TRUE
              em: TRUE
    contentheader:
      type: string
      ui:
        label: 'contentheader'
        inlineEditable: TRUE
        reloadIfChanged: TRUE
        inline:
          editorOptions:
            placeholder: 'Enter title...'
            formatting:
              left: TRUE
              center: TRUE
              right: TRUE
              strong: TRUE
              em: TRUE
    content:
      type: string
      ui:
        label: 'content'
        inlineEditable: TRUE
        reloadIfChanged: TRUE
        inline:
          editorOptions:
            placeholder: 'Enter contacts...'
            formatting:
              left: TRUE
              center: TRUE
              right: TRUE
              strong: TRUE
              em: TRUE
              a: TRUE
    extrainfo:
      type: string
      ui:
        label: 'extrainfo'
        inlineEditable: TRUE
        reloadIfChanged: TRUE
        inline:
          editorOptions:
            placeholder: 'Optional...'
            formatting:
              left: TRUE
              center: TRUE
              right: TRUE
              strong: TRUE
              em: TRUE

'Vendor.WeltenSchaffenAt:TermsOfUse':
  superTypes:
    'Neos.Neos:Document': true
  ui:
    icon: 'icon-gavel'
    label: 'WeltenSchaffenAt Terms of use'

# If you need more than just the "main" content area you can uncomment the snippet below. It will add a "sidebar" area
# to your pages. You can add as many areas as you like, still only those that are rendered in your Fusion code and
# template are visible on your page.

#    'sidebar':
#      type: 'Neos.Neos:ContentCollection'
